import { test, module } from 'qunit';
import Compiler from 'graphql-adapter/compiler';

module('unit:graphql-adapter/compiler');

test("it works", function(assert) {
  assert.equal(Compiler, Compiler);
});
